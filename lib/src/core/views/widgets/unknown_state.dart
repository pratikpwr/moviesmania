import 'package:flutter/material.dart';

class UnKnownState extends StatelessWidget {
  const UnKnownState({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
